// src/config.ts

export const API_BASE_URL = "https://stagingapi.binarytech.io";

// Optional: add other shared constants here
export const STORAGE_KEYS = {
  token: "access_token",
  orgId: "organisationId",
};

export const DEFAULT_PAGE_SIZE = 50;

export const CALLER_COMPONENT = "ManifestMapMobile"; // used in headers